{
  "name": "backend-service",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf apps/**/dist lib/**/dist",
    "dev:blog": "pnpm --filter blog-service dev"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}